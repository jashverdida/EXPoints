# ─────────────────────────────────────────────────────────────────────────────
# EXPoints – Environment Variable Template
# Copy this file to .env for local development (never commit .env to git).
# For Vercel, set each variable in: Project → Settings → Environment Variables
# ─────────────────────────────────────────────────────────────────────────────

# ── Application ───────────────────────────────────────────────────────────────
APP_ENV=production          # "production" on Vercel, "development" locally
APP_DEBUG=false
APP_URL=https://your-app.vercel.app

# ── Cloud MySQL Database ───────────────────────────────────────────────────────
# Recommended providers (all have a free tier):
#   • PlanetScale  https://planetscale.com   (serverless MySQL, best Vercel DX)
#   • Railway      https://railway.app        (MySQL container)
#   • TiDB Cloud   https://tidbcloud.com      (MySQL-compatible)
#
# After creating your database run:
#   mysql -h <DB_HOST> -u <DB_USER> -p<DB_PASS> <DB_NAME> < setup-complete-posts-system.sql
#   mysql -h <DB_HOST> -u <DB_USER> -p<DB_PASS> <DB_NAME> < setup-sessions-table.sql
#
DB_HOST=your-db-host.example.com
DB_NAME=expoints_db
DB_USER=your_db_user
DB_PASS=your_db_password
DB_PORT=3306

# ── Supabase (configured but not yet wired into PHP – future use) ──────────────
SUPABASE_URL=https://katkmdwiaketfmcmadxa.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ── Session ───────────────────────────────────────────────────────────────────
SESSION_LIFETIME=7200

# ── Legacy / local-only ────────────────────────────────────────────────────────
# These are only used when running locally against a MySQL server.
# DB_HOST overrides these on the cloud.
# DB_HOST=127.0.0.1
# DB_NAME=expoints_db
# DB_USER=root
# DB_PASS=
